{
	"name": "cmd-speculative",
	"private": true,
	"workspaces": [
		"digital-passport",
		"slideshow"
	],
	"scripts": {
		"dev": "turbo run dev --parallel",
		"build": "turbo run build",
		"lint": "turbo run lint"
	},
	"devDependencies": {
		"turbo": "^1.12.3"
	}
} 